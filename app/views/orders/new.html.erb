<h2>Passe ta commande !</h2>

<%= form_for :order, url: orders_path do |f| %>
    <p>
      <%= f.label :content %> <br>
      <%= f.text_area :content %>
    </p>

    <p>
      <%= f.label :author %> <br>
      <%= f.text_field :author %>
    </p>

    <%= f.submit %>
<% end %>